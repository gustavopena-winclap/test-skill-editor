# Greeting Card Style Reference

Styles accepted by `scripts/greet.py --style`:

| style | description                          |
|-------|--------------------------------------|
| box   | message wrapped in an ASCII box      |
| plain | single line, no decoration           |
| stars | message framed by `*` characters     |

Default style: `box`.
